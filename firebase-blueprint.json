{
  "entities": {
    "Message": {
      "title": "Message",
      "description": "A chat message between a user and IT support.",
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The content of the message."
        },
        "sender": {
          "type": "string",
          "description": "The name of the person who sent the message."
        },
        "timestamp": {
          "type": "number",
          "description": "The time the message was sent (Unix timestamp)."
        },
        "role": {
          "type": "string",
          "enum": ["user", "it"],
          "description": "The role of the sender."
        },
        "sector": {
          "type": "string",
          "description": "The sector of the user (if applicable)."
        },
        "uid": {
          "type": "string",
          "description": "The Firebase Auth UID of the sender."
        }
      },
      "required": ["text", "sender", "timestamp", "role", "uid"]
    }
  },
  "firestore": {
    "/messages/{messageId}": {
      "schema": "Message",
      "description": "Global collection of chat messages for support."
    }
  }
}
